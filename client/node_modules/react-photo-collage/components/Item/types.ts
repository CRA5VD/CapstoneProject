import { Props as MaskProps } from '../Mask/types';

export interface ItemProps {
    source: string;
    itemWidth: string;
    itemHeight: string;
    onClick?: () => void;
}

export interface ItemWithMaskProps extends ItemProps, MaskProps {}