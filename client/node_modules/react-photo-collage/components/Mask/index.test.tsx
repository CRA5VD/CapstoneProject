import React from 'react';
import renderer from 'react-test-renderer';

import Mask from './index';

test('snapshot test', () => {
    const tree = renderer.create(<Mask remainingPhotos={11} />).toJSON();
    expect(tree).toMatchSnapshot();
});