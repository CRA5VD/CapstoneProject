import React from 'react';

import { Props } from './types';
import { SC_Mask, SC_NumOfRemaining } from './index.sc';

const Mask: React.FC<Props> = (props) => {
    const { remainingPhotos } = props;

    return (
        <SC_Mask>
            <SC_NumOfRemaining>{remainingPhotos}</SC_NumOfRemaining>
        </SC_Mask>
    );
}

export default Mask;