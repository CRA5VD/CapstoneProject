import styled from 'styled-components';

export const SC_Mask = styled.div`
    width: 100%;
    height: 100%;
    position: absolute;
    cursor: pointer;
    z-index: 1;
    background-color: rgba(0, 0, 0, .4);
`;

export const SC_NumOfRemaining = styled.div`
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    font-size: 35px;
    color: #fff;
    &::before {
        content: '+'
    }
`;